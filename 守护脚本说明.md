# 校园跑腿 · 后端守护脚本使用说明

> 位置：`D:\miniprogram123`
> 作用：**后端挂了自动重启** + **自动把 cpolar 公网地址写进小程序**

---

## 一、三个文件分别是什么

| 文件 | 作用 | 要不要动它 |
| --- | --- | --- |
| `启动后端守护.bat` | 双击运行入口（可见窗口，能看到实时日志） | 双击即可，不用改 |
| `auto-restart-backend.ps1` | 守护程序本体（检查 + 自动重启 + 同步地址） | 正常不用改 |
| `cpolar-url.txt` | cpolar 公网地址配置（自动检测失败时手动填这里） | 只有提示检测失败时才需要填 |

运行日志：`D:\miniprogram123\logs\keep-alive-日期.log`

---

## 二、怎么用

### 1. 平时（推荐）
双击 `启动后端守护.bat`。
窗口里会打印实时状态，**这个窗口不要关**（关了守护就停了）。
要停止：在这个窗口按 `Ctrl + C`，或直接关掉窗口。

### 2. 只想启动一次（不守护）
```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File D:\miniprogram123\auto-restart-backend.ps1 -NoWatch
```

### 3. 手动指定 cpolar 地址并立刻写入小程序
```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File D:\miniprogram123\auto-restart-backend.ps1 -CpolarUrl https://xxxx.r36.cpolar.top -NoWatch
```

### 4. 想改检查频率 / 失败几次才重启
```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File D:\miniprogram123\auto-restart-backend.ps1 -CheckIntervalSec 10 -FailThreshold 3
```
默认：每 10 秒检查一次，连续 3 次失败（约 30 秒）就自动重启后端。

---

## 三、它能自动做哪些事

1. **后端保活**：每 10 秒请求 `http://127.0.0.1:3000/api/health`，
   连续 3 次不通 → 自动杀掉占用 3000 端口的旧进程 → 重新 `node src/app.js` 拉起后端。
   （只杀占用 3000 端口的进程，不会误杀你电脑上其它 node 程序）
2. **MySQL 自愈**：发现 `MySQL80` 服务没运行会尝试自动启动（需要管理员权限）。
3. **cpolar 地址自动同步**：把可用的公网地址写入
   `miniprogram\utils\request.js` 里的 `BASE_URL`（写入前自动备份到 `logs\`），
   微信开发者工具会自动重新编译，真机预览/体验版重新点一次「预览」即可。
   地址没变化时不会做任何修改。
4. **单实例保护**：重复双击不会启动第二个守护进程。
5. **启动即自检**：脚本一启动就会检查并补齐后端 + 地址。

---

## 四、cpolar 公网地址是怎么自动拿到的

按下面顺序依次尝试，**每个地址都会先请求 `<地址>/api/health` 验证，验证通过才会写入小程序**：

1. `cpolar-url.txt` 里手动填写的地址（最稳，优先用）；
2. 小程序 `request.js` 里当前已配置的地址（能用就不改）；
3. cpolar 日志（`C:\Users\liqiud\.cpolar\logs`）里最近出现过的 `https://xxx.cpolar.top`。

### 如果三种都失败（脚本提示「未能自动确认 cpolar 公网地址」）
1. 浏览器打开 <http://localhost:9200> （Cpolar Web UI）；
2. 左侧「隧道管理」→ 找到本地端口 **3000** 的那条隧道；
3. 复制它的 https 地址（形如 `https://xxxx.r36.cpolar.top`）；
4. 粘贴到 `D:\miniprogram123\cpolar-url.txt` 里 `#URL#` 那一行，保存。

保存后**1 分钟内脚本会自动检测并写入小程序**，不用重启脚本。

> 小提示：`cpolar-url.txt` 会自动把当前生效的地址回写在 `#URL#` 后面，方便核对。

---

## 五、常见问题

**Q：小程序里地址被改了，怎么回退？**
每次修改前都会备份到 `D:\miniprogram123\logs\request.js.时间戳.bak`，把它复制回
`miniprogram\utils\request.js` 即可。

**Q：想让电脑一开机就自动守护？**
按 `Win + R` 输入 `shell:startup` 回车，把 `启动后端守护.bat` 的**快捷方式**放进去即可。

**Q：怎么确认守护在跑？**
看图个地方：`logs\keep-alive-日期.log` 有今天的日志，且日志里有「进入守护模式」。

**Q：后端一直起不来？**
看 `backend\logs\server-*.err.log`（后端报错）和 `backend\logs\server-*.out.log`（运行日志），
常见原因是 MySQL 没启动（`MySQL80` 服务）或 3306 端口被占用。
